# TODO

## Before

Copy /etc/kernel/*cmdline variables
Copy /etc/mkinitcpio.d/linux.preset and /etc/mkninitcpio.conf
Copy service drop in files in /etc/systemd/system 
Copy /etc/fstab

Check active services

Check pacman and AUR packages

## After
firewall - docker
ananicy - cachyos

Paste /etc/mkinitcpio.d/linux.preset WITH mkinitcpio.conf
