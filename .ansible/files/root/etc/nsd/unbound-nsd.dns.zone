$ORIGIN unbound-nsd.dns.
$TTL 86400 ; default time-to-live for this zone

unbound-nsd.dns. IN SOA ns.unbound-nsd.dns. admin.unbound-nsd.dns. (
    2020080302 ;Serial
    7200       ;Refresh
    3600       ;Retry
    1209600    ;Expire
    3600       ;Negative response caching TTL
)

; The nameservers that are authoritative for this zone.
                 NS   unbound-nsd.dns.

; A and AAAA records are for IPv4 and IPv6 addresses respectively
unbound-nsd.dns. A    127.0.0.1
                 AAAA ::1
